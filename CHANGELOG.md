# 变更记录

### v0.6.1
1. 修复从其他窗口切回浏览器时页面出现灰色遮罩、需刷新才消失的问题(Dialog 切屏动画残留)

### v0.6.0
1. 新增 AI 代理配置页(超管可见):配置 worker 访问 OpenAI 的 HTTP 代理,含三档连通性测试
2. 修复分类成功后前端不切到提议态的问题(单批场景未推送 SSE 就绪事件)
3. 修复分类失败后状态未持久化、重连看不到失败提示的问题
4. docker 容器内可经 host.docker.internal 访问宿主机代理

### v0.5.8
1. 前端 UI 重构:启用明暗主题切换、统一品牌头、落地页 hero 区、工作台图标化
2. 移除已绑 QQ 时冗余的「加载我的喜欢」按钮

### v0.5.7
1. 修复 uvicorn 日志 logger 名显示为「-」的问题
2. 日志初始化提前到 import 时,worker/beat 日志也走 loguru 持久化

### v0.5.6
1. 「我喜欢」订阅 QQ 音乐接口偶发超时改为自动重试 1 次,减少 502

### v0.5.5
1. 「我喜欢」订阅 QQ 音乐偶发读超时改为返回 502 + 中文提示
2. 修复日志异常 traceback 泄露 QQ 凭证明文的问题
3. QQ 音乐接口 read 超时从 10s 放宽到 30s

### v0.5.4
1. 前端错误展示重构:API 错误用 toast 通知、表单校验就近提示、分类失败用模态框
2. 新增 shadcn Dialog 组件

### v0.5.3
1. AI_API_KEY 校验所有环境都拒绝占位符,dev 下启动即报错而非调 AI 时才报

### v0.5.2
1. 修复 nginx 反代 backend 容器重建后 502 的问题(改用动态解析)

### v0.5.1
1. 修复每用户每日分类上限未生效的问题(死代码),日限真正启用
2. songs 超量校验提前到限流之前,改数量后可立即重试
3. IP 限流收窄到注册/登录端点,修复 NAT 网络下正常用户被误限
4. 429 响应提示中文化

### v0.5.0
1. 新增分批分类:超 200 首自动切批,各批独立分类后合并,支持全量 2000 首
2. 分类启动改为异步:立即返回,进度通过 SSE 推送
3. 每用户每日分类上限 30→10

### v0.4.0
1. 前端 UI 重构:引入 shadcn/ui + Tailwind v4,主色改青绿
2. 前端状态管理重构:TanStack Query + Zustand 替代散落 useState
3. 全部页面用 shadcn 组件重写,移除旧手写 CSS

### v0.3.0
1. 「我喜欢」歌曲拉取去除 500 首截断,改为全量拉取
2. 修复分享歌单仅返回前 50 首的问题(补全分页)

### v0.2.2
1. CLAUDE.md 新增开发流程章节
2. .gitignore 合并至根目录

### v0.2.1
1. 修复 .env 迁移后文档残留路径
2. CONTRIBUTING 环境变量表补全

### v0.2.0
1. 新增版本管理机制(VERSION 文件 + 后端运行时版本暴露)
2. 配置层集中:根 .env + .env.example,docker/后端/前端共用
3. 生产环境密钥强制校验:启动即拒绝默认/空值
4. 前端配置层 config.ts,VITE_ 环境变量可配

### v0.1.0
1. 邀请码注册 + 邮箱密码登录(JWT 鉴权)
2. QQ 扫码登录态服务端持久化(自动刷新 musickey)
3. 「我喜欢」/分享歌单双入口取歌,按 song_id 精确添加保证版本一致性
4. AI 多轮 HITL 分类(LangGraph + Redis checkpoint,最多 5 轮反馈)
5. Celery + Redis 任务队列
6. Docker Compose 一键部署
7. loguru 日志 + 文件持久化 + rotation
8. 邀请码 + 公开注册开关 + 用户级/IP 级限流
